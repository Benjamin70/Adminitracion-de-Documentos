@model Capa_Entidad.Departamentos

@{
    ViewBag.Title = "Eliminar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h3>¿Esta Seguro que desea eliminar este Departamento?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            <label>Identificador Departamento</label>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Id_Dpto)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nombre)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Siglas)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Siglas)
        </dd>
    </dl>

    @using (Html.BeginForm("Eliminar", "Departamentos", FormMethod.Post)) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <a class="btn btn-primary" href="@Url.Action("Index", "Departamentos")"> <i  class="glyphicon glyphicon-menu-left" ></i> Regresar al Listado</a>
            <input type="submit" value="Eliminar" class="btn btn-danger" /> 
        </div>
    }
</div>
